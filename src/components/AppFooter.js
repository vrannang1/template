import React from "react";

export default function AppFooter() {
  return (
    <footer>
      <p>
        Get away for some learnin' with
        {` `}
        <a href="https://pragmaticstudio.com">The Pragmatic Studio</a>
        {` `}
        <span role="img" aria-label="Fishing">
          🏖
        </span>
      </p>
    </footer>
  );
}
